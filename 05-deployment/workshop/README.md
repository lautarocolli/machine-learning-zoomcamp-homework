Workshop from the Zoomcamp that updates the content, using different, more modern tools, to perform the same tasks. In particular, we use:
* Scikit-Learn pipelines
* uv instead of Pipenv
* FastAPI instead of Flask
* Fly.io instead of AWS EBS